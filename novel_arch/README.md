# Novel Architecture
The new architecture will be developed here which will be a Graph Neural Network (GNN).

train.py has a standard function to train an inputted model, train_archs.py has functions to call to train a specific architecture.