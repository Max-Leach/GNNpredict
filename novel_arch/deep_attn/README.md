# Deep Attention

The architecture here is based on GIN + graph attention methods.
It has the option to use deeper layers for graph node updates and the option to use attention, both of which can be removed from the model via specifying different parameters.